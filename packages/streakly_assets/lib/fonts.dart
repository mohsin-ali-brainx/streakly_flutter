/// Font family names registered in `pubspec.yaml` under `flutter/fonts`.
/// Add matching `.ttf` / `.otf` files under `fonts/` before using.
abstract final class StreaklyFonts {
  StreaklyFonts._();

  // Example after registering fonts in this package's pubspec:
  // static const String display = 'StreaklyDisplay';
}
